import { Lift } from './lift';
import { LiftBuilder } from './lift-builder';

describe('Lifts', () => {
  it('should create an instance', () => {
    //expect(new Lift(new LiftBuilder('Curl')))
  });
});
